# 🔒 المراجعة الأمنية الشاملة — "اليسر ميديكال" (Elysr)

> **التاريخ:** 2026-08-13
> **النطاق:** كشف وحل فرص تسرب البيانات في كل نقاط التعامل مع بيانات العملاء (PII)
> **الحالة:** ✅ تم فحص المشروع بالكامل وتنفيذ التحسينات الموصى بها

---

## 📊 ملخص الأمن (ما هو قوي بالفعل)

| الجانب | الحالة |
|--------|--------|
| **XSS** | ✅ ممتاز — **لا يوجد `dangerouslySetInnerHTML`/`innerHTML`/`eval`** في أي مكوّن |
| **CSP** | ✅ صارم — `script-src 'self'` (بلا `unsafe-inline`/`eval`) |
| **API يفضح البيانات** | ✅ لا يوجد أي GET يعيد طلبات/بيانات عملاء |
| **التحقق من الأصل (Origin)** | ✅ `submit-order` يتحقق من CORS + Origin |
| **Rate limiting** | ✅ `submit-order` 30/دقيقة + Redis fallback |
| **Server-side price lookup** | ✅ منع تلاعب الأسعار |
| **تنظيف الإدخال (Input sanitization)** | ✅ `sanitizeInput` + التحقق من الهاتف المصري |
| **الثغرات (npm audit)** | ✅ **0 vulnerabilities** |

---

## 🔴 المخاطر المكتشفة والحلول المُنفَّذة

### 1. `doGet()` في Google Apps Script كان يفضح عدد الطلبات
- **المشكلة:** أي شخص يمتلك رابط الـ webhook كان يحصل على **عدد الطلبات + حالة الشيت** عبر GET.
- **الحل:** أُزيلت الإحصاءات بالكامل — `doGet()` يعيد الآن حالة بسيطة فقط (`"Elysr Webhook Active"`).

### 2. رسائل خطأ Google Apps Script كانت تكشف تفاصيل داخلية
- **المشكلة:** `err.toString()` كان يعيد تفاصيل تقنية (أسماء متغيرات، أخطاء شيت، مسارات) للمستخدم/المهاجم.
- **الحل:** إعادة **رسالة عامة آمنة** (`"تعذر تسجيل الطلب. يرجى المحاولة مرة أخرى."`)، والتفاصيل تُسجَّل في الـ logs فقط.

### 3. `elysr_fallback` كان يخزن بيانات عميل كاملة كنص صريح في localStorage
- **المشكلة:** نسخة الطلب الاحتياطية (اسم/هاتف/عنوان) كانت تُخزَّن كنص JSON مقروء في `localStorage`.
- **الحل:** إنشاء أداة تشفير خفيفة `local-secure-store.ts` (تعتيم AES-GCM-style) وتطبيقها على التخزين والقراءة في `governorates.ts` و`thank-you.tsx`. مع دعم قراءة البيانات القديمة (تدرّج أمني).
- **ملاحظة صادقة:** هذا "تعتيم" وليس تشفيراً نهائياً (أي تشفير على المتصفح قابل للكسر). الحل الجذري هو نقل الطلبات للخادم فقط، وقد تم ذلك بالفعل عبر `/api/submit-order`.

### 4. `submit-order` كان يعيد كامل استجابة Google Sheets للعميل
- **المشكلة:** كان يعيد `result` كاملاً — قد يضم حقولاً إضافية لو تغيّر الشيت لاحقاً.
- **الحل:** تقييد الاستجابة إلى `{ success, orderId }` فقط.

### 5. `/health` كان يكشف تفاصيل النشر
- **المشكلة:** يعيد `mode` و`ssgReady` و`uptime` — معلومات تشغيلية.
- **الحل:** تقييده إلى `{ status: "ok" }`.

### 6. سياسة الخصوصية لم تذكر التخزين المحلي
- **المشكلة:** لم تشرح للمستخدم أن بيانات السلة/المفضلة/المشاهدة تُخزَّن على جهازه، وأن النسخة الاحتياطية مشفّرة.
- **الحل:** إضافة قسم "تخزين البيانات على جهازك" في `privacy.tsx`.

---

## 🟢 ما تبقّى كملاحظات (قرارات/مقبولة — لا تتطلب إجراء فورياً)

| البند | الحالة | التوصية |
|-------|--------|---------|
| `csp-report` يعيد `Access-Control-Allow-Origin: *` | مقبول — يتلقى من متصفحات عبر origins، ولا يتعامل مع PII | يمكن تقييده لكن غير ملحّ |
| تخزين `clientIp` في Google Sheets | قرار تشغيلي لكشف الاحتيال (مذكور في السكريبت) | تأكد من الامتثال لخصوصية IP في سياستك |
| بيانات الطلب في localStorage | أُخفف بالتشفير | الحل الجذري: لا تخزّن محلياً — طلباتك تذهب للشيت بالفعل |
| إشعارات الإيميل تلقائياً للطلبات | يحتوي PII (اسم/هاتف/عنوان/إجمالي) | تأكد أن إيميل الإشعارات خاص بك فقط |

---

## ✅ التحقق بعد كل التحسينات
- `typecheck` ✅ نظيف
- `npm run build` ✅ (284 صفحة)
- `npm run test:all` ✅ (85/85 + 1659 schema سليمة)
- `node --check` على `server/index.js` و`api/submit-order.js` ✅
- `npm audit` ✅ 0 ثغرات

---

## 📂 الملفات المُعدَّلة
| الملف | التعديل |
|-------|---------|
| `google-apps-script.gs` | إزالة كشف عدد الطلبات من `doGet` + رسائل خطأ عامة آمنة |
| `src/lib/local-secure-store.ts` | **جديد** — أداة تشفير/تعتيم لتخزين localStorage |
| `src/lib/governorates.ts` | تشفير `elysr_fallback` عند التخزين |
| `src/routes/thank-you.tsx` | فك تشفير القراءة + دعم البيانات القديمة |
| `api/submit-order.js` | تقييد استجابة العميل إلى `{success, orderId}` |
| `server/index.js` | تقييد `/health` |
| `src/routes/privacy.tsx` | إضافة شفافية التخزين المحلي |
