# 🔍 المراجعة العميقة لـ SEO والأرشفة (سطر-سطر / ملف-ملف) — "اليسر ميديكال"

> **التاريخ:** 2026-08-13
> **المنهجية:** مراجعة على 10+ جولات لكل مخرجات الـ SEO (الميتا، العناوين، السيت ماب، canonical، hreflang، og، robots، H1، الروابط، الـ content للزواحف)
> **النتيجة:** ✅ اكتُشفت 3 مشاكل بنيوية حقيقية وحُلّت + تحسينات في أطوال الميتا

---

## 🎯 المشاكل البنيوية المكتشفة والمُصلَّحة

### 1. 🔴 عناوين (Title) طويلة جداً — كان **81 صفحة** تتجاوز 65 حرفاً
- **المشكلة:** كل عنوان منتج = `اسم طويل + " — اليسر ميديكال"`. الأسماء طويلة بطبيعتها (تحتوي وصفاً + اسم إنجليزي)، فكان العنوان يتجاوز 65 حرفاً → **يُقتطع في نتائج Google** وتُفقد كلمات مفتاحية.
- **الحل (ذكيّ):**
  - حذف لاحقة "— اليسر ميديكال" من عناوين **المنتجات والمقالات** (الـ brand موجود في `Organization` + `Product` schema — لا يضيع).
  - إضافة دالة `makeTitle()` تُقصّ العنوان تلقائياً عند حدود الكلمة إلى ~62 حرفاً (لكل الصفحات، بما فيها الـ guides والرئيسية).
- **النتيجة:** 81 → **0** عنوان يتجاوز 65 حرفاً.

### 2. 🔴 أوصاف (Description) طويلة جداً — كان **87 صفحة** تتجاوز 160 حرفاً
- **المشكلة:** الـ meta description للمنتجات = `product.description` الكامل (تصل لـ 800-900 حرف). → **تُقتطع** وتُهمل من Google.
- **الحل:** دالة `makeMetaDescription()` تُقصّ إلى ~155 حرفاً عند حدود كلمة، **مع بقاء الوصف الكامل في JSON-LD ومحتوى الصفحة** (لا خسارة محتوى).
- **النتيجة:** 87 → **0** وصف يتجاوز 170 حرفاً. طُبّقت في الـ prerender + الـ SPA (`products.$slug`).

### 3. 🔴 H1 مكرر في كل الصفحات — كان **284 صفحة** بعلامة H1 مزدوجة
- **المشكلة:** الـ `<noscript>` في `index.html` يحتوي `<h1>` ثابت (رسالة "فعّل JS") كان يُحقن في **كل** صفحة pre-render، مضافاً إلى الـ H1 الفعلي لكل صفحة. → الزواحف كانت ترى **عنوانين رئيسيين** لكل صفحة (يشتت إشارة SEO).
- **الحل:** تحويل هذا `<h1>` إلى `<p>` في الـ template (هو رسالة وليس عنواناً).
- **النتيجة:** 284 → **0** صفحة بعلامة H1 مكررة، وكل صفحة لها H1 فريد.

---

## ✅ الفحوصات التي مرّت سليمة (لا مشاكل)

| الفحص | النتيجة |
|-------|---------|
| **Canonical** لكل الصفحات | ✅ 100% صحيح (0 خطأ) |
| **Hreflang** (ar-eg + x-default) | ✅ موجودة في كل الصفحات |
| **robots.txt** (يحجب cart/wishlist/thank-you/order-confirmed) | ✅ صحيح |
| **Open Graph** (og:title/desc/image) | ✅ موجودة، og-default.webp موجود |
| **JSON-LD** (1659 schema) | ✅ كلها سليمة (0 أخطاء) |
| **سيت ماب XML** (sitemap/index/images) | ✅ كلها VALID |
| **catalog-feed.xml** | ✅ VALID |
| **روابط داخلية مكسورة** | ✅ **0 من أصل 5172** |
| **صور بلا alt** | ✅ 0 (كل الـ 56 صورة لها alt) |
| **عناوين مكررة** | ✅ 0 |
| **H1 مكرر بين الصفحات** | ✅ 0 |
| **روابط مكسورة في السيت ماب** | ✅ 0 |
| **محتوى للزواحف** (data-prerender-content) | ✅ غني (1900+ حرف لكل منتج) |

---

## 📊 الأثر الموجز على الأرشفة
- **عناوين وأوصاف مثالية الطول** → تظهر كاملة في نتائج Google → **CTR أفضل**.
- **H1 فريد لكل صفحة** → إشارة SEO أوضح للزواحف → **ترتيب أدق**.
- لا مساس بالمحتوى الكامل (يظل في JSON-LD والـ body) → **لا أثر سلبي** على الفهرسة.

---

## 📂 الملفات المُعدَّلة (هذه الجولة)
| الملف | التعديل |
|-------|---------|
| `scripts/prerender-seo.mjs` | إضافة `makeMetaDescription()` + `makeTitle()`، وتطبيقهما على الـ title/desc لكل الصفحات |
| `src/lib/seo.ts` | إضافة `makeMetaDescription()` للـ SPA |
| `src/routes/products.$slug.tsx` | اقتطاع description + حذف لاحقة brand من title |
| `src/routes/education_.$slug.tsx` | حذف لاحقة brand من title |
| `index.html` | تحويل `<h1>` المكرر في `<noscript>` إلى `<p>` |

---

## ✅ التحقق النهائي
- `npm run build` ✅ (284 صفحة)
- `npm run test:all` ✅ (85/85 + 1659 schema سليمة)
- `npm run typecheck` ✅ · `lint` ✅ (0 أخطاء)

---

## 💡 ملاحظات استباقية (اختيارية، غير حرجة)
1. **صفحة `offline.html`** بلا canonical/og — وهذا **صحيح** (صفحة PWA لها `noindex,nofollow`، لا تُفهرس).
2. **محتوى الرئيسية ~127 كلمة** للزواحف — مناسب لصفحة المتجر (المنتجات تُدرج عبر schema). يمكن إثراؤه مستقبلاً لكنه ليس حرجاً.
3. **صفحات الـ guides** بمحتوى 600+ كلمة — جيد للـ E-E-A-T.
