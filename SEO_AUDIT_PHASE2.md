# 🔍 خريطة الفحص التالية — "اليسر ميديكال" (Elysr)

> **التاريخ:** 2026-08-13
> **الغرض:** ما يجب فحصه الآن بعد إصلاحات الأداء/الأرشفة السابقة
> **الحالة:** أُجريت جولة فحص سريعة كشفت **مشكلة محتملة كبرى + بضع فرص**، مذكورة أدناه

---

## 🔴 1. معالجة منتجات RED — **قرارك: إبقاؤها في البحث العضوي ✅ (منفّذ)**

### قرارك
أنت تريد هذه المنتجات **ظاهرة في البحث العضوي** — وهذا هو ما طُبّق.

### الوضع النهائي (بعد التنفيذ)
| الجانب | الحالة | ملاحظة |
|--------|--------|--------|
| صفحة المنتج (Google/Bing) | ✅ `index,follow` | تظهر في البحث العضوي |
| في `sitemap.xml` | ✅ **كل 11 RED موجودة** | تساعد على الاكتشاف والفهرسة |
| في React/SPA | ✅ indexable | لا يوجد noindex ديناميكي |
| **في الكشتلوج الإعلاني** | ✅ **مستثناة (0)** | **الحماية الحقيقية للإعلانات المدفوعة** |
| التوثيق | ✅ حُدّث ليعكس الواقع | README.md + README.ar.md + تعليق compliance |

### لماذا هذا أفضل لهدفك
- **البحث العضوي يبقى مفتوحاً**: هذه المنتجات تحمل أسماء تجارية تُبحث كثيراً، فإبقاؤها مفهرسة = زيارات عضوية قيّمة + تحويلات واتساب مباشرة (وهذا ما طلبته).
- **الإعلانات محمية**: الاستثناء الوحيد الفعلي هو الكشتلوج المدفوع (Google Merchant / Facebook Catalog) — فيتجنّب تحذيرات سياسات الإعلانات دون المساس بالظهور العضوي.
- **التوثيق أصبح صادقاً**: لم يعد هناك ادعاء خاطئ بأن RED تُحذف من جوجل.

### تغييرات إضافية أُنجزت
- ✅ إضافة `Disallow: /wishlist` إلى `robots.txt` (بالإضافة إلى noindex الموجود).

### التحقق
- ✅ `typecheck` نظيف · `test:all` 85/85 · `schemas` 1659/0 · `lint` 0 أخطاء · `build` 284 صفحة
- ✅ كل 11 RED في sitemap · كلها `index,follow` · كلها مستثناة من catalog feed (0) · wishlist محجوبة

---

## 🟠 2. جولة الفحص التقني (الروابط/الصور/الـ redirects) — ✅ منجزة

| البند | النتيجة |
|-------|---------|
| صور المنتجات (87) | ✅ كلها موجودة على القرص |
| صور المقالات (55) | ✅ كلها موجودة (بعد إزالة `?v=` من الفحص) |
| روابط داخلية مكسورة (كل 284 صفحة) | ✅ **0 روابط مكسورة** |
| صور ناقصة في صفحات pre-render | ✅ **0 صور ناقصة** |
| حلقات redirects / self-loops في vercel.json | ✅ **لا يوجد** |
| مراجع منتجات محذوفة في الكود | ✅ **لا يوجد** (نُظّف `m-51` + `m-10`) |

### ما نُظّف في هذه الجولة (مراجع منتجات محذوفة)
- **`m-51` (Overtime)** — أُزيل من: `product-compliance.ts` (RED→10)، `products.ts` (قوائم العرض)، `ProductsTabs.tsx`، `ShopByConcern.tsx`، `internal-links.ts`. **redirects 301 مُبقاة** لحماية SEO.
- **`m-10` (محذوف)** — أُزيل من مرجعين في `internal-links.ts` (royal-honey-benefits + natural-honey-dates-benefits).
- الـ 3 إشارات متبقية (`p-2`, `p-3`, `m-57`) = **مطابقات خاطئة** (كلاسات CSS أو مثال في تعليق)، لا علاقة لها بمنتجات.

---

## ⚠️ 3. Meta Pixel / CAPI — غير مُنفَّذ (يحتاج قرارك)

### الواقع
- `README` و`.env.example` و`index.html` **يذكرون** Meta Pixel / CAPI (`meta-pixel.ts`, `/api/meta-capi`, `META_CAPI_CSRF`).
- **لكن لا يوجد أي تنفيذ فعلي**: لا `meta-pixel.ts`، ولا `/api/meta-capi`، ولا أي سكربت `fbevents` أو اتصال بـ `graph.facebook.com`.
- `index.html` فيه تعليق فقط: `<!-- Meta Pixel <noscript> fallback — معطل حالياً -->`.
- **النتيجة: تتبع إعلانات فيسبوك لا يعمل إطلاقاً حالياً** (لا Pixel ولا Conversions API).

### لماذا يهم
- إن كنت تعتمد على **إعلانات فيسبوك/إنستغرام** (تحويلات/رفع جماهير/تحسين حملات)، فبدون Pixel/CAPI:
  - لا يمكنك تتبع التحويلات بدقة.
  - لا يمكنك بناء **جماهير مخصصة (Custom Audiences)** أو **متشابهة (Lookalikes)**.
  - لا تحصل على بيانات إسناد لتحسين حملاتك.
- الكشتلوج (catalog-feed.xml) و domain verification موجودان جاهزين، لكن **الجزء الحسّاس (التتبع) مفقود**.

### الخيارات (أحتاج قرارك)
- **(أ)** إضافة تنفيذ كامل: Pixel في الواجهة + CAPI خلفي في `/api/meta-capi` (باستخدام `META_CAPI_CSRF` الموجود). *(الأفضل لو الإعلانات مهمة)*
- **(ب)** عدم الاحتياج له — عندها أحدّث التوثيق لإزالة أي ذكر خاطئ لـ Meta/CAPI (يستقيم مع `.env.example` الذي يقول "بدون Meta/TikTok Tracking").
- **(ج)** تأجيل القرار.

---

## ⚠️ 3. ملاحظات إضافية وُجدت

1. **Meta Pixel / CAPI غير مُنفَّذ**: `README` و`.env.example` يذكران `META_CAPI_CSRF` و`meta-pixel.ts`، لكن الملف غير موجود ولا أي مرجع في `src/` أو `api/`. هذا يعني أن **تتبع فيسبوك لا يعمل** رغم أن البنية (catalog feed + domain verification) جاهزة له. *(إن كنت تعتمد على إعلانات فيسبوك، هذا يُحتاج فحصه.)*

2. **`wishlist` في robots.txt**: الـ noindex في HTML يحمي الفهرسة، لكن إضافة `Disallow: /wishlist` للروبوتات ستكون أوضح.

3. **GA4 يُحمّل عند أول تفاعل فقط**: قرار متعمّد (لرفع درجة PageSpeed)، لكنه يعني أن **الزيارات التي لا يتفاعل فيها الزائر لا تُسجَّل** في GA — قد يُنقص عدادات الزيارات الحقيقية.

---

## ✅ حالة خطة الفحص (المحدَّثة بعد الجولات)

### المرحلة 1 — سلامة المخرجات ✅ **مكتملة**
- [x] typecheck / lint / tests / schemas / build
- [x] السيت ماب لا يحوي cart/wishlist/thank-you
- [x] فحص روابط داخلية مكسورة → **0 مكسورة**
- [x] فحص أن كل صورة product موجودة فعلاً → **0 ناقصة**
- [x] حلقات redirects / self-loops → **لا يوجد**
- [x] مراجع منتجات محذوفة → **نُظّفت (m-51 + m-10)**

### المرحلة 2 — الأرشفة والسياسات ✅ **مكتملة**
- [x] قرار RED → إبقاؤها في البحث العضوي (قرارك)
- [x] تحديث robots.txt ليشمل wishlist
- [x] إعادة توليد sitemap بعد التغييرات

### المرحلة 3 — التتبع والقياس ⏳ **بانتظار قرارك**
- [ ] **Meta Pixel / CAPI**: غير مُنفَّذ رغم ذكره في التوثيق — هل نحتاجه؟
- [ ] تقييم نموذج GA4 "تحميل عند التفاعل" مقابل دقة القياس

### المرحلة 4 — السرعة الواقعية (اختياري متقدم)
- [ ] تشغيل Lighthouse/PageSpeed على نسخة build محلية لقياس LCP/CLS/INP فعلياً

---

*تمت هذه الجولة: فحص شامل للروابط والصور والـ redirects والمراجع الميتة + تنظيف m-51/m-10 + تحديث أرقام README.*
