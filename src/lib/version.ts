/**
 * ============================================================
 * 🔖 رقم إصدار الموقع — مصدر الحقيقة الوحيد
 * ============================================================
 * يُقرأ من `package.json` (حقل version) حتى لا يتكرر الرقم يدوياً
 * في أكثر من مكان. أي تحديث للإصدار يتم في package.json فقط،
 * وتنتشر القيمة تلقائياً هنا وفي error-tracking.
 *
 * يُرفع الإصدار عند إصدار release عبر سكربت `npm run release`.
 * ============================================================
 */
import pkg from "../../package.json";

/** رقم إصدار الموقع الحالي (من package.json). */
export const APP_VERSION: string = pkg.version ?? "0.0.0";
