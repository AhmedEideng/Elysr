# 📋 مراجعة سجلات الخادم — "اليسر ميديكال" (Elysr)

> **التاريخ:** 2026-08-13
> **النطاق:** مراجعة كل نقاط تسجيل السجلات في السيرفر (`server/index.js`) ووحدات الـ API (`api/`) ووحدات الـ scripts — بحثاً عن تسجيل بيانات حساسة (PII) أو تسجيل زائد.

---

## 🗺️ أين تذهب السجلات فعلياً
- **لا توجد ملفات سجل مخصصة** (لا `appendFile`/`writeFile` للسجلات). كل التسجيل عبر `console.*`.
- على **Vercel** (الوضع الفعلي) يذهب `console.*` إلى **سجلات الـ Functions** في لوحة Vercel.
- على **الاستضافة الذاتية** (`server/index.js`) يذهب إلى stdout/stderr للعملية.

---

## 📊 النتائج حسب الملف

### ✅ سليم (لا بيانات حساسة)
| الملف | التسجيل | الحالة |
|-------|---------|--------|
| `server/index.js` | أخطاء boot + معلومات تشغيل عامة (Port/Mode/SSG/Redis) | ✅ لا PII |
| `api/lib/rate-limiter.js` | حالة Redis فقط (متصل/فشل/fallback) + `err.message` | ✅ لا PII |
| `scripts/*.mjs` | أخطاء بناء/معالجة عامة | ✅ لا PII |
| `scripts/auto-generate-article.mjs` | حالة توليد + `err.message` | ✅ لا يسجّل مفتاح Gemini |

### 🟡 نقاط فُحصت وتبيّن أنها آمنة لكن حسّنتها
| الملف | السطر | ما كان يُسجَّل | التعديل |
|-------|-------|---------------|---------|
| `api/submit-order.js` | 254 | **رد Google Sheets كاملاً** (قد يحوي بيانات) | تقييده إلى أول **200 حرف** |
| `api/csp-report.js` | — | كان يقبل body بلا حد حجم، ويسجّل حقول قادمة من المهاجم بلا تعقيم | أضفت **حد 4KB** + **تعقيم** الحقول (`blockedUri`/`documentUri`/`directive`/`scriptSample`) لمنع **Log Injection** |

### 🔴 أهم ما وجدته وأصلحته
1. **`submit-order` كان يمرر الرد الكامل من Google Sheets إلى السجل** — لو احتوى الشيت بيانات إضافية أو اعترضه شيء، قد تُسجَّل بيانات. → الآن أول 200 حرف فقط.
2. **`csp-report` كان يقبل body بلا حد** — يُستخدم كـ DoS (استهلاك ذاكرة) أو **Log Flooding** (حشر سجلات ضخمة). → الآن حد 4KB.
3. **`csp-report` كان يسجّل قيماً خام من المهاجم** (`documentUri`/`blockedUri`) — **Log Injection** محتمل (حقن سطور تحكم في السجلات). → الآن تُعقَّم (تُزال `\r\n\t\0`) وتُقتطع.

---

## 🔐 أفضل الممارسات المرتبطة
- **لا تُسجَّل بيانات العملاء (اسم/هاتف/عنوان) في أي سجل خادم** ✅ — الطلبات تُرسل إلى الشيت، ولا يُسجَّل محتواها.
- **لا تُسجَّل الأسرار** ✅ — `GEMINI_API_KEY`/`GOOGLE_SHEETS_WEBHOOK_URL` تُفحص فقط (Missing) ولا تُطبع قيمها. مفتاح Gemini يظهر في URL الطلب (ممارسة شائعة لكنها قد تظهر في proxy logs — سكربت يدوي لا في مسار الطلب).
- `clientIp` يُخزَّن في الشيت (قرار تشغيلي لكشف الاحتيال) ولا يُسجَّل في console.

---

## ✅ التحقق بعد التعديلات
- `node --check` على `api/csp-report.js` + `api/submit-order.js` ✅
- `typecheck` ✅ · `test:all` (85/85) ✅ · `lint` (0 أخطاء) ✅ · `build` (284 صفحة) ✅

---

## 📂 الملفات المُعدَّلة
| الملف | التعديل |
|-------|---------|
| `api/submit-order.js` | تقييد رد الشيت المسجَّل إلى أول 200 حرف |
| `api/csp-report.js` | حد حجم body 4KB + تعقيم الحقول ضد Log Injection |
