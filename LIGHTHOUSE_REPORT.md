# 🚦 تقرير Lighthouse — "اليسر ميديكال" (Elysr)

> **التاريخ:** 2026-08-13
> **البيئة:** محاكاة Lighthouse افتراضية (Slow 4G) على نسخة build محلية (`vite preview`)
> **الأداة:** Lighthouse (Chrome headless) · الفئات: Performance / SEO / Best-practices / Accessibility
>
> **📌 ملاحظة النتائج:** درجات الأداء متقلبة بين الجولات (±5–7) بسبب طبيعة محاكاة Slow 4G.
> القيم أدناه نطاقات من عدة جولات.

---

## 📊 النتائج

### 🏠 الصفحة الرئيسية
| الفئة | النتيجة |
|-------|---------|
| **Performance** | **71 / 100** |
| **SEO** | **100 / 100** ✅ |
| **Best Practices** | 96 / 100 |
| **Accessibility** | 100 / 100 ✅ |

| المقياس (Core Web Vitals) | القيمة | التقييم |
|---------------------------|--------|---------|
| LCP (أكبر عنصر مرسوم) | **4.8 s** | ⚠️ الأضعف |
| FCP | 2.7 s | متوسط |
| CLS (استقرار التخطيط) | **0** | ✅ ممتاز |
| TBT (حجب التفاعل) | 310 ms | متوسط |

### 🛍️ صفحة منتج (Hammer of Thor)
| الفئة | النتيجة |
|-------|---------|
| **Performance** | **73 / 100** |
| **SEO** | **100 / 100** ✅ |
| Best Practices | 96 / 100 |
| Accessibility | 94 / 100 |

| المقياس | القيمة |
|---------|--------|
| LCP | **4.5 s** |
| FCP | 2.9 s |
| CLS | **0** ✅ |
| TBT | 280 ms |

---

## ✅ ما هو ممتاز بالفعل
- **SEO = 100/100** في الصفحتين (الـ meta، canonical، JSON-LD، السيت ماب كلها سليمة).
- **Accessibility = 100/100** (الهوم) — إمكانية الوصول ممتازة.
- **CLS = 0** في الصفحتين — لا أي قفز في التخطيط (إنجاز نادر وممتاز للأرشفة/التجربة).
- **Best Practices = 96/100**.

---

## ⚠️ أين المشكلة: LCP (4.5–4.8 s)

### السبب الجذري (من فحص الـ HTML المُولَّد)
- صفحة الهوم **pre-rendered** لكن الـ HTML النهائي **لا يحتوي على أي عنصر `<img>`** — كل الصور (الـ hero + المنتجات) تُحقن **بعد تشغيل JavaScript** (عبر React hydration).
- يوجد `<link rel="preload">` للـ hero فقط في الـ head، لكن **عنصر الصورة الفعلي يُضاف متأخراً** بعد تنفيذ JS.
- على اتصال Slow 4G المُحاكى، هذا التتابع (تحميل HTML → تشغيل JS → هيدرة React → إضافة الصورة → رسمها) يرفع LCP إلى ~4.8s.
- **السبب ليس حجم الصور** (الـ hero 49 KB فقط) بل **تأخر ظهورها بسبب اعتمادها على الـ JS**.

### ملاحظة مهمة (سياق عادل)
- Lighthouse يحاكي **Slow 4G** (نطاق ~1.6 Mbps + 150ms تأخير). على اتصال حقيقي عبر **Vercel Edge CDN** سيكون الأداء أسرع بكثير.
- هذا النمط (CSR بعد هيدرة) هو **مقايضة مقصودة** في معمارية المشروع: SEO يتم بالكامل عبر pre-render (النصوص في `noscript`)، بينما الصور تُرسم تفاعلياً.

---

## 🎯 خيارات تحسين LCP — نتيجة التجربة والتراجع

### ما نُفِّذ وجرّب ثم أُعيد (الأمان أولاً)
**جرّبت الخيار (أ):** إدراج صورة `<img>` للـ hero مباشرة في الـ HTML المُولَّد (داخل `#root`).
- ✅ **تحسّن FCP** من ~2.7s إلى ~2.0s (الصورة ظهرت مبكراً في الـ DOM).
- ⚠️ **لكن ظهرت CLS** (حتى 0.5) لأن React عند الـ hydration تستبدل `#root` بمحتواها (Header + Hero) فتنزاح العناصر — **قفزة تخطيط تضرّ بالأرشفة والـ UX**.
- ⚠️ **LCP لم يتحسن** (ظل ~4.6–4.8) لأن عنصر LCP النهائي هو صورة الـ Hero التي تُنشئها React **بعد اكتمال تشغيل JS** — في معمارية CSR يرتبط LCP بوقت تنفيذ الـ bundle.

**القرار:** **أُعيدت التغييرات** للاحتفاظ بـ **CLS=0** (المكسب الأهم). أي محاولة لإدراج صورة مبكرة تتطلب إعادة معمارية (SSR حقيقي) تُولّد الـ Hero في الـ HTML بنفس البنية النهائية — وهي مخاطرة كبيرة لا تستحق.

### الوضع النهائي المستقر (بعد التراجع)
| المقياس | الهوم | المنتج |
|---------|-------|--------|
| **Performance** | 69–77 (متوسط ~73) | ~73 |
| **SEO** | **100** ✅ | **100** ✅ |
| **CLS** | **0** ✅ | **0** ✅ |
| LCP | ~4.8 s | ~4.5 s |
| FCP | ~2.6 s | ~2.9 s |
| TBT | 140–460 ms | ~280 ms |

> 💡 **الخلاصة الصادقة:** في معمارية CSR (React تعرض كل شيء بعد تشغيل JS)، **LCP ~4.5–4.8s على محاكاة Slow 4G هو حدّ طبيعي** لا يمكن تجاوزه جوهرياً دون إعادة بناء إلى SSR. على اتصال حقيقي عبر **Vercel Edge CDN** الأداء أسرع بكثير. والنتائج الحالية ممتازة للمتجر: **SEO 100/100، CLS=0 (مثالي)، Accessibility 100/100**.
>
> **التحسينات التي حقّقتها فعلياً** (في الجولات السابقة): تقليص المسار الحرج للهوم من **292 kB → 168 kB gzip** (chunk البيانات 879→254 kB)، مما يخفّض زمن تنفيذ JS وبالتالي LCP/TBT حقيقياً على أي اتصال.

---

*تشغيل: `vite preview` محلي + Lighthouse (Chrome headless) على الهوم وصفحة منتج. ملفات JSON الخام: `lighthouse-home.json`، `lighthouse-product.json`.*
