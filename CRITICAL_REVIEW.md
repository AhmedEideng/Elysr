# 🔍 المراجعة العميقة للمشاكل الحرجة (سطر-سطر / ملف-ملف) — "اليسر ميديكال"

> **التاريخ:** 2026-08-14
> **المنهجية:** مراجعة على 28+ جولة لكل منطق العمل، الحالات الحافة، الاتساق، الأداء، والبيانات.
> **النتيجة:** ✅ اكتُشفت 3 مشاكل حرجة حقيقية وحُلّت + تحسينات إضافية، وتم نفي عدة مخاطر محتملة.

---

## 🔴 المشاكل الحرجة المكتشفة والمُصلَّحة

### 1. "شاهدتها مؤخراً" (Recently Viewed) كان يعرض منتجات محذوفة/بيانات قديمة
- **الملف:** `src/components/sections/RecentlyViewed.tsx`
- **المشكلة:** كان يعرض المنتجات مباشرة من `localStorage` (slug/سعر/اسم/صورة) **دون التحقق من وجودها في الكتالوج الحالي**. لو حُذف منتج من المتجر (مثل m-51 الذي حذفناه) أو تغيّر سعره، سيظهر:
  - **رابط يؤدي إلى 404** (منتج محذوف).
  - **سعر/اسم قديم** لمنتج تغيّرت بياناته.
- **الحل:** إضافة تصفية عبر `getProductBySlug()` — تُستبعد المنتجات غير الموجودة، وتُعرض البيانات **المحدّثة** من الكتالوج الحالي.
- **الأثر على الأداء:** لا تراجع — `data-catalog` كان يُسحب أصلاً في الهوم (عبر FeaturedProducts/ProductsTabs)، فالتعديل لا يضيف وزناً.

### 2. "أحدث المقالات" (ArticlesGrid) كان يعلق كـ skeleton forever لو فشل التحميل
- **الملف:** `src/components/sections/ArticlesGrid.tsx`
- **المشكلة:** التحميل الكسول لـ `@/data/articles` بلا `.catch` — لو فشل الـ import (خطأ نادر)، يبقى `featured=[]` → **4 مربعات رمادية دائمة** أسفل الصفحة.
- **الحل:** إضافة `.catch` يُخفي الـ skeleton عند الفشل.

### 3. اعتماد غير مستخدم `web-vitals`
- **الملف:** `package.json`
- **المشكلة:** `web-vitals` مدرج كاعتماد لكنه **غير مستخدم في أي مكان** في الكود (لا import).
- **الحل:** إزالته (`npm uninstall web-vitals`) — تنظيف آمن.

---

## ✅ المخاطر التي فُحصت ونُفيت (لا مشاكل — تطابق مثالي)

| الفحص | النتيجة |
|-------|---------|
| **منطق الخصم** (العميل vs الخادم) | ✅ متطابق تماماً (نفس الـ tiers + `Math.round`) |
| **تكلفة الشحن** (27 محافظة) | ✅ متطابقة 100% بين `governorates.ts` و`config-db.json` |
| **شحن مجاني** (2000+) | ✅ متطابق (كلاهما يستخدم `subtotal >= 2000`) |
| **نوع الطلب** (cart/شراء فوري) | ✅ متطابق |
| **generateOrderId** | ✅ فريد (timestamp + randomUUID) |
| **مراجع المنتجات** في `products.ts`/`internal-links`/`landing-pages`/`cross-sell` | ✅ كلها لمنتجات موجودة (0 مرجع لمنتجات محذوفة) |
| **مراجع المقالات** في `internal-links` | ✅ كلها لمقالات موجودة (51/51) |
| **بيانات المنتجات** (اسم/سعر/وصف/صورة/تقييم) | ✅ كلها مكتملة (0 مشكلة) |
| **سلغات/أسماء مكررة** | ✅ 0 |
| **`getFeaturedProducts`** | ✅ يعيد 6 منتجات صحيحة |
| **الفئات** (men/women/devices) | ✅ 56/24/7 = 87 صحيحة |
| **صفحات الـ prerender** على الخادم | ✅ تُقدَّم بمحتوى كامل (ليس SPA shell فقط) |
| **`/wishlist` (ديناميكي)** | ✅ يقدم SPA shell صحيح |
| **العد التنازلي للعروض** (`getTimeLeft`) | ✅ منطق سليم (يتجدد كل 3 أيام) |
| **منتجات نفدت من المخزون** | ✅ 0 |
| **`productIdToSlug`** | ✅ موجود (إرث) لكن كل روابط الواجهة تستخدم slug — لا مشكلة |

---

## 🗂️ الملفات المُعدَّلة (هذه الجولة)
| الملف | التعديل |
|-------|---------|
| `src/components/sections/RecentlyViewed.tsx` | تصفية المنتجات المحذوفة + عرض البيانات المحدّثة من الكتالوج |
| `src/components/sections/ArticlesGrid.tsx` | معالجة فشل التحميل الكسول (إخفاء skeleton) |
| `package.json` + `package-lock.json` | إزالة اعتماد `web-vitals` غير المستخدم |

---

## ✅ التحقق النهائي
- `npm run typecheck` ✅ · `npm run lint` ✅ (0 أخطاء) · `npm run build` ✅ (284 صفحة) · `npm audit` ✅ (0 ثغرات)
- `npm run test:all` ✅ (85/85 + 1659 schema سليمة)

---

## 📌 ملاحظات إضافية (اختيارية، غير حرجة)
- `web-vitals` أُزيل — لكن لو أراد مالك المشروع إضافة Core Web Vitals tracking لاحقاً، يمكن إعادة استخدامها في `__root.tsx`.
- `RecentlyViewed` و`Wishlist` الآن كلاهما يصفّي المنتجات المحذوفة — متسقان.
- صحيح أن `data-catalog` (254KB) في المسار الحرج للهوم بسبب FeaturedProducts/ProductsTabs، لكنها أقسام أساسية للهوم، فالأداء مقبول (SEO 100/100، CLS 0).
