# Elysr — تقرير التدقيق والتنفيذ

**تاريخ التدقيق:** 18 سبتمبر 2026 — Africa/Cairo
**المستودع:** `main`، نقطة البداية `5334f6e`
**نطاق التدقيق:** التطبيق، الكتالوج، المراجعات، APIs، Google Apps Script، SEO/JSON-LD، sitemap/feed، الأمن، الاختبارات، البناء، ومسارات الإنتاج.

## الخلاصة التنفيذية

كان السبب الأخطر في ضعف الثقة العضوية ليس مشكلة أداء، بل نشر تقييمات وعدادات غير صادرة عن عملاء حقيقيين في عدة طبقات: بيانات الكتالوج، بطاقة المنتج، صفحة المنتج، wishlist، مكون تقييمات اصطناعي، اختبارات، وProduct JSON-LD. تم إزالة هذا المسار بالكامل من نسخة الكود الحالية.

تم أيضاً إصلاح ثلاث مشكلات SEO/فهرسة مستقلة، وإكمال تسجيل كود الإحالة في مسار الطلب بدلاً من تجاهله، وتقوية استخراج IP وحدود مخازن rate-limit، وإزالة ادعاءات رئيسية غير موثقة من SEO/about/prerender والواجهات الأساسية.

> **مهم:** الإنتاج الحالي لم يُنشر من هذه الشجرة بعد. لذلك يعكس الفحص الحي بعض السلوك القديم، خصوصاً `aggregateRating` في صفحات المنتجات وإدراج `catalog-feed.xml` في sitemap index. يجب تنفيذ النشر ثم إعادة فحوص ما بعد النشر الواردة أدناه.

## الإصلاحات المنفذة

### 1. المراجعات والتقييمات

- حُذف المسار الاصطناعي بالكامل:
  - `src/features/product/components/ProductReviews.tsx`
  - `src/lib/product-reviews.ts`
  - `src/__tests__/product-reviews.test.ts`
- بقي `CustomerReviews` المسار الوحيد لعرض مراجعات العملاء، ويقرأ من `/api/reviews` المراجعات المعتمدة فقط.
- لم يعد الكتالوج يحتوي حقلي `rating` أو `reviews` على مستوى المنتجات؛ تم حذفهما من ملفات الرجال والنساء والأجهزة، ومن `Product` type، ومن `api/lib/products-db.json` المولد.
- أزيلت النجوم والعدادات من `ProductCard` و`ProductImage` وصفحة المنتج وwishlist. لا تظهر عبارة مثل `0 تقييم` ولا aggregate وهمي.
- أزيل اعتماد ترتيب المنتجات على `rating × reviews`.
- أضيف حارس integrity يمنع عودة أي من حقلي `rating` و`reviews` إلى الكتالوج.
- أضيفت اختبارات صريحة لـ JSON-LD تثبت:
  - لا يوجد `aggregateRating` من دون ملخص مراجعات معتمد.
  - لا يقبل الملخص إلا `ratingValue` بين 1 و5 و`reviewCount` موجباً.
- `src/lib/seo.ts` و`scripts/prerender-seo.mjs` لا يصدران aggregate ثابتاً. نقطة إدخاله المستقبلية مشروطة بملخص API معتمد، ولا يوجد caller حالي يحقنه.

### 2. SEO وsitemap/feed

- أزيل `reviewedBy` من Article JSON-LD؛ فحص CI الآلي ليس مراجعة بشرية مستقلة.
- أزيلت من prerender صياغات التقييم الثابتة وعبارات التسويق غير المدعومة في الصفحة الرئيسية والصفحات الأساسية.
- أزيلت من Organization JSON-LD معلومات تأسيس وعدد موظفين غير موثقة، وأصبح `sameAs` يضم ملفاً اجتماعياً فقط.
- إصلاح sitemap index: `catalog-feed.xml` feed تجاري وليس sitemap، فلم يعد مدرجاً في `sitemap-index.xml` أو robots كـ Sitemap.
- إصلاح sitemap الصور: كان مقال الصورة يكرر `<image:loc>` مرتين؛ أصبح لكل entry موقع صورة واحد.
- إصلاح `lastmod`: عند غياب Git metadata لا يخترع تاريخ البناء تاريخ تعديل للمحتوى؛ يُحذف الوسم بدلاً من إرسال إشارة زائفة. وتستخدم صفحات الفئات آخر تعديل فعلي للكتالوج عند الحاجة.
- حافظ feed على 78 منتجاً مؤهلاً.

### 3. الإحالة

كان `referralCode` يخرج من الواجهة لكنه يُهمل في API وGoogle Sheet. تم:

- التحقق منه server-side بنمط `EL-[A-Z0-9]{4,8}` وتوحيده uppercase.
- إضافته إلى allowlist في `api/submit-order.js`.
- إضافته إلى عمود `كود الإحالة` في `google-apps-script.gs`؛ السكربت يضيف العمود الناقص عند تشغيل النسخة الجديدة.
- إزالة وعد الخصم التلقائي من صفحة الإحالة ورسالة المشاركة وتوضيح أن الكود يسجل مصدر الإحالة، وأي مكافأة تخضع لسياسة المتجر.

### 4. الأمن وrate limiting

- إنشاء `api/lib/request-ip.js` كمصدر مشترك.
- لا تُوثق `x-forwarded-for` في النشر المباشر/self-hosted. يُوثق `x-vercel-ip` فقط عندما يعمل الكود فعلياً داخل Vercel (`VERCEL=1`)؛ وإلا يستخدم عنوان socket.
- أضيف سقف 20,000 entry مع eviction للمخازن الداخلية في rate limiter، وCSP reports، وerror reports؛ هذا يمنع نمو الذاكرة بلا حد عبر identifiers متغيرة.
- أضيفت اختبارات لمساري Vercel وnon-Vercel لاستخراج IP.

### 5. جودة الكتالوج

- حدّثت رابط Mayo Clinic القديم في مصادر المقالات إلى المسار الحالي؛ فحص المصادر الآن 59 live + مصدر واحد محجوب آلياً بلا روابط ميتة.
- أصلح تناقض `m-09 / royal-cream` بين وصف يوحي بمنتج فموي وتعليمات استخدام موضعي؛ أصبح الوصف موضعياً ومحافظاً.
- أصبح فحص التناقضات يرجع صفر مخالفات.
- نقلت قواعد تطبيع السلة إلى `src/lib/cart-normalization.ts` وأزيلت تحذيرات Fast Refresh من `contexts/cart.tsx` دون تغيير السلوك.

## التحقق المحلي

| الفحص                          |                                                                النتيجة |
| ------------------------------ | ---------------------------------------------------------------------: |
| `npm run lint`                 |                                            ناجح، 0 errors / 0 warnings |
| `npm run typecheck`            |                                                                   ناجح |
| `npm run test`                 |                              ناجح: integrity + security headers parity |
| `npm run test:unit`            |                                          **257 passed**، 20 test files |
| `npm run build`                | ناجح: 17 static + 78 products + 58 articles + 93 guides = **246 صفحة** |
| `npm run test:schemas`         |             **0 errors، 0 warnings**، 1,159 JSON-LD schema في 247 HTML |
| `npm run test:e2e`             |                                                          **19 passed** |
| `npm run test:all`             |                                                           ناجح بالكامل |
| `npm audit --audit-level=high` |                                                      0 vulnerabilities |
| `npm run check-source-links`   |                        60 unique URLs: 59 live + 1 bot-blocked, 0 dead |
| `npm run test:sources`         |          Safe explicit skip because no generated-article marker exists |
| `git diff --check`             |                                                                   ناجح |

أثناء اختبارات الوحدة ظهر log متوقع لاختبارات timeout/CSP و`Not implemented: navigation to another Document` من jsdom؛ لا توجد failures.

## فحص الإنتاج بعد التعديل

تم فحص `https://elysrmedical.store` مرة أخرى، لكن النشر لم يتغير بعد:

- `/` = 200
- `/robots.txt`، `/sitemap.xml`، `/sitemap-index.xml`، `/sitemap-images.xml`، `/catalog-feed.xml` = 200
- `/api/reviews?product=m-01` = 200
- مسار غير معروف = 404 حقيقية
- production feed = 78 items
- production sitemap = 240 URLs
- production image sitemap = 136 entries
- الصفحة الرئيسية الحية لا تحتوي aggregate، لكن PDP القديم ما زال يحتويه؛ مثال `products/kreva-gel-for-men` ما زال يعرض aggregate و`73 تقييم`.
- production `sitemap-index.xml` ما زال يحتوي `catalog-feed.xml`.

النسخة المحلية بعد build:

- sitemap = 240 URLs
- image sitemap = 136 entries، وموقع صورة واحد لكل entry
- feed = 78 items
- sitemap index لا يحتوي feed
- صفحات `dist/products` لا تحتوي `aggregateRating`
- `api/lib/products-db.json` لا يحتوي حقول catalog للتقييمات

## خطوات النشر الإلزامية

1. نشر commit هذه الشجرة إلى Vercel/النشر المعتمد.
2. نشر إصدار جديد من `google-apps-script.gs` وتشغيل/التأكد من `setupAutoCleanupTrigger` حسب الإجراء التشغيلي الموجود في السكربت.
3. تنفيذ طلب تجريبي عبر referral والتأكد من ظهور عمود `كود الإحالة` وقيمته في Sheet.
4. بعد انتهاء CDN cache، إعادة فحص:
   - صفحة منتج HTML: لا `aggregateRating` ولا `reviewCount` ولا `73 تقييم`.
   - `sitemap-index.xml`: لا `catalog-feed.xml`.
   - `sitemap-images.xml`: لا تكرار `image:loc`.
   - `/api/reviews`: المراجعات المعتمدة فقط.
   - طلب حقيقي/اختبار داخلي: referral code يصل إلى Sheet.
5. في Google Search Console: أعد إرسال `sitemap-index.xml`، وراجع تقارير Page indexing وEnhancements بعد الزحف. لا تضع Merchant feed داخل sitemap؛ اربطه في Merchant Center فقط.

## فصل مشاكل الكود عن الانتشار الخارجي

### تم إصلاحه في الكود

- structured-data spam الناتج عن التقييمات المصطنعة.
- feed داخل sitemap index.
- duplicate image sitemap locations.
- lastmod غير الموثوق عند غياب Git.
- صفحات/بطاقات تعرض أرقام تقييم غير قابلة للإثبات.
- referral attribution المفقود.
- IP/rate-limit risks المذكورة أعلاه.

### إجراءات خارجية لازمة ولا ينفذها commit وحده

- Google Search Console: فحص Pages/Manual actions/Enhancements، طلب إعادة فهرسة للصفحات الأساسية بعد النشر، ومراجعة canonical/robots.
- Merchant Center: إبقاء `catalog-feed.xml` كمصدر feed مستقل ومراجعة diagnostics ورفض المنتجات.
- GA4/Search Console: فصل brand queries عن non-brand، ومقارنة CTR وimpressions وindexing قبل/بعد إزالة النجوم.
- الانتشار: بناء روابط حقيقية من مصادر موثوقة، توزيع المقالات التوعوية ذات المصادر، وتحسين صفحات الهبوط بحسب استعلامات Search Console الفعلية؛ لا يمكن تعويض ذلك بنصوص أو تقييمات مولدة.

## مخاطر متبقية ذات أولوية

- بعض نصوص الكتالوج/صفحات الدليل التجارية تحتاج دورة تحرير يدوية مستمرة لإثبات كل ادعاء أصالة/استيراد/نتيجة، حتى لو مرّت حارس العبارات الحالي. يجب ربط كل claim تجاري بدليل تشغيلي أو تخفيفه.
- `aggregateRating` لن يظهر تلقائياً بعد وصول مراجعات API؛ وهذا مقصود للحذر. إذا تقرر لاحقاً تفعيله، يجب جلب ملخص approved من نفس API، إسناده للمنتج الصحيح، وإضافة اختبار E2E/schema يثبت التطابق بين المعروض والـ JSON-LD.
- rate limiter ما زال in-process؛ الحماية العابرة لكل instances تحتاج Redis/KV أو WAF إذا ارتفع الحجم. حدود Apps Script تبقى طبقة ثانية.
- بيانات الموقع الحية لن تتغير قبل النشر وإخلاء cache؛ لا تعتبر فحوص production الحالية دليلاً على تطبيق الإصلاحات فيها.
